# API Reference

This page should contain endpoint reference and models. Start by documenting routes in `app/routes/` and models in `app/models/`.

Suggested sections to add:

- Authentication and health checks
- Scheduling endpoints
- Solver endpoints
- Models and example payloads
